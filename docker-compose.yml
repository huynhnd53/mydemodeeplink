version: '3.8'

services:
  nginx:
    container_name: deeplink-nginx
    image: nginx:latest
    ports:
      - '3333:3333'
    build:
    volumes:
      - '.:/usr/share/nginx/html'
      - './nginx.conf:/etc/nginx/conf.d/default.conf'
    restart: always